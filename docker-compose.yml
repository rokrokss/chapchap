version: '3'
services:
  db:
    image: postgres:15
    container_name: my-postgres
    environment:
      POSTGRES_PASSWORD: secretpassword
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
